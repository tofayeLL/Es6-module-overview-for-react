

const add = (first, second) => {
    return first + second;
}

const multiply = (first, second) => {
    return first * second;
}
const divideFirstNumberByTheSecondNumber = (first, second) => {
    return first / second;
}

export { add, multiply, divideFirstNumberByTheSecondNumber }